# API Gateway Configuration
server.port=8081
spring.application.name=api-gateway

# Gateway Routes are typically defined in code, but can be externalized here
# Add custom gateway configuration as needed
